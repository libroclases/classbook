import { HoraValidatorDirective } from './hora-validator.directive';

describe('HoraValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HoraValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
