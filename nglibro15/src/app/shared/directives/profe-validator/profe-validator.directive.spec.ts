import { ProfeValidatorsDirective } from './profe-validator.directive';

describe('ProfeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfeValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
