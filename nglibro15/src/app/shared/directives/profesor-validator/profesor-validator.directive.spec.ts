import { ProfesorValidatorDirective } from './profesor-validator.directive';

describe('ProfesorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfesorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
